1.1- install docker on the Device then run docker
1.2- try to docker is working with "docker info" in commend line 
2- load main project from git (link)
3- open the project then run the comment "docker-compose up --build -d"
3.0 if need to rebuild the image can use "docker build -t <image-name> .
"
3.! after add some new comment in Dockerfile can use docker build -t sitename . to update



PHPUnit 
1- open Bash -> docker-compose exec php bash
2- ./vendor/bin/phpunit ./app/tests/filename.php
